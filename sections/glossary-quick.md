# FPF Quick Glossary (Top 50 Terms)

Auto-generated from metadata.json. Maps core terms to their primary pattern.

| Term | Primary Pattern | Plain Definition |
|------|----------------|------------------|
| evidence | A.10 | Links every claim to its sources so it can be checked and replayed |
| edition pins | G.0 | Fix exact versions used, so results can be reproduced later |
| provenance | A.2.4 | Records where knowledge came from, so trust is traceable |
| RSCRTriggerKindId | G.0 | Names what event forces a re-check of published comparisons |
| composition | A.9 | Rules for combining parts so totals stay consistent at every scale |
| promise content | A.2.3 | States what a consumer is promised, apart from who does the work |
| scope | A.6.6 | Says where and when a statement applies, preventing overreach |
| invariants | A.9 | Rules that must hold at every scale, so zooming never breaks logic |
| source return | C.30.AD | Points readers back to originals so descriptions never replace them |
| TypedSetViews | A.0 | Shows results as labeled sets, not one winner, keeping options visible |
| context | A.2 | Bounds a statement to one setting so words keep a single meaning |
| evidence carrier | A.2.9 | Separates the record from the act it reports, avoiding false proof |
| CN-Spec | A.6.7 | Fixes how a quantity is defined and measured so values compare fairly |
| CG-Spec | A.6.7 | Sets which comparisons are legal, so rankings can be audited |
| viability envelope | A.6.8 | Marks the conditions under which a promise stays deliverable |
| mereology | A.6.H | Clarifies part-whole talk so "whole" claims mean one exact thing |
| reopen | A.16 | Lets a settled decision be revisited when new signals arrive |
| polarity | A.18 | Says whether higher values are better or worse, avoiding flipped scores |
| tri-state admissibility (`pass\|degrade\|abstain`) | A.19.UINDM | Lets checks pass, warn, or opt out instead of forcing yes/no |
| penalties→`R_eff` only | A.19.ULSAM | Applies doubts to confidence, never to the measured values themselves |
| trust | B.1.3 | Keeps combined knowledge no more certain than its weakest input |
| assurance | B.3 | Turns "we trust it" into a claim with evidence, scope, and limits |
| formality | B.3 | Grades how rigorously a claim is stated, from sketch to proof |
| grounding | B.3.5 | Links a readable claim to the trace that shows why to believe it |
| lost structure | C.29 | Names what a model ignores, so its limits stay visible |
| selected structure | C.30 | Picks which parts matter for a design question, ignoring the rest |
| architecture structural view | C.30.AD | Keeps diagrams honest: views describe a system, never replace it |
| Pedagogy | E.5.3 | Keeps teaching material from reshaping the stable core it explains |
| ReferencePlane | G.0 | Declares the common ground two things are compared on |
| DeclaredSubstrateInterpretiveView | A.0 | Makes the chosen reading of raw data explicit, not assumed |
| role | A.2 | Names what something is being in a setting, apart from who fills it |
| effective ReferenceScheme | A.2.1 | Fixes which vocabulary is in force when reading an assignment |
| currentness | A.2.2 | Checks a fact or skill is still valid now, not just once true |
| state machine | A.2.5 | Tracks which state something is in, so actions fire only when ready |
| method-description membership | A.3.2 | Tests whether a document really tells how a method is done |
| evolution | A.4 | Builds in feedback loops so designs improve when reality talks back |
| modularity | A.5 | Keeps a stable core small; new domains plug in without breaking it |
| publication face | A.6 | Separates what is published from the thing itself, avoiding confusion |
| atomic L/A/D/E claims | A.6 | Splits mixed boundary text into single-purpose, checkable statements |
| RelationSignature | A.6.P | Declares a relation's participants and types before it is used |
| SlotSpec | A.6.P | Types each participant slot so relations can't be wired wrongly |
| interface | A.6.RSIR | Forces "interface" to mean one exact thing before design relies on it |
| port | A.6.RSIR | Pins down what a connection point is before wiring depends on it |
| work | A.6.F | Distinguishes work actually done from what something is meant to do |
| substitutability | A.6.M | Tests whether a part can be swapped without breaking the whole |
| anchoring | A.6.6 | Ties a dependent claim to its explicit base, scope, and witnesses |
| suite obligations | A.6.7 | Lists shared duties every mechanism in a set must meet |
| planned baseline | A.6.7 | Records the intended setup so later changes can be compared to it |
| crossing visibility | A.6.7 | Makes hand-offs across boundaries explicit, so nothing moves unseen |
| exact F.17 `SchemeSenseCell` endpoints | A.6.9 | Bridges link exact local meanings, not vague "same thing" claims |
